
{
  "name": "alfadz-muslim",
  "version": "1.0.0",
  "description": "Alfadz Muslim Progressive Web App",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step required'",
    "copy": "npx cap copy",
    "sync": "npx cap sync",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
